{# <nav class="navbar navbar-expand-lg" style="background-color: rgb(20,33,61); color:white;">
  <a class="navbar-brand" href="/">PAKI PAKI</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('home') }}">Restaurants</a>
      </li>
      {% if app.user %}
       <li class="nav-item">
        <a class="nav-link" href="{{ path('profil.show') }}">Profil</a>
      </li>
       <li class="nav-item">
        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
      </li>
      {% else %}
            <li class="nav-item">
                <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
            </li>
        </ul>
      {% endif %}
    
  </div>
</nav>
<style>
  .nav-link, .navbar-brand {
    color : white;
  }
</style> #}


<nav class="navbar navbar-expand-lg navbar-dark">
  <a class="navbar-brand" href="{{ path('search') }}"><img src="../img/logo.png" alt="PAKI PAKI" class="logo" style="width:40px;"></a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href=""><span class="sr-only">(current)</span></a>
      </li>
    </ul>
      <ul class="navbar-nav justify-content-end">

        <li class="nav-item active">
          <a class="nav-link" href="{{ path('home') }}">Restaurants <span class="sr-only">(current)</span></a>
        </li>
        
        {% if app.user %}

          <li class="nav-item">
            <a class="nav-link" href="{{ path('profil.show') }}">
              <svg class="bi bi-person-fill" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                <path fill-rule="evenodd" d="M3 14s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1H3zm5-6a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd"/>
              </svg>
            </a>
          </li>

          <li class="nav-item">
            <a class="nav-link"  href="{{ path('app_logout') }}">
              <svg class="bi bi-power" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                <path fill-rule="evenodd" d="M5.578 4.437a5 5 0 104.922.044l.5-.866a6 6 0 11-5.908-.053l.486.875z" clip-rule="evenodd"/>
                <path fill-rule="evenodd" d="M7.5 8V1h1v7h-1z" clip-rule="evenodd"/>
              </svg>
            </a>
          </li>

          {% else %}

            <li class="nav-item active">
              <a class="nav-link" href="{{ path('app_login') }}">Connexion <span class="sr-only">(current)</span></a>
            </li>

          {% endif %}
    </ul>
  </div>
</nav>

<style>
  nav {
    background-color: rgb(20,33,61) ;
  }

</style>