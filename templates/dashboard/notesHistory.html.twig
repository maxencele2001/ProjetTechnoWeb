{% extends 'base.html.twig' %}

{% block title %}Historique de notes{% endblock %}


{% block body %}

    <div class="container pt-5">
        <div class="row">
            <div class="col-12">
                
                <h1>Historique de note</h1> 
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('admin.dashboard') }}">Tableau de Bord</a></li>
                    <li class="breadcrumb-item active">Historique de notes</li>
                </ol>
            
                {% for note in notes %}           
                    <div class="cart shadow p-3 mb-5 bg-white rounded">
                        <div class="cart-content p-4">
                            <div class="d-flex justify-content-around">
                                <div class="id">#{{ note.id }}</div>
                                <div class="date"><b>Note :</b> {{ note.note }}</div>
                                <div class="user"><b>User :</b> {{ note.user.name }} {{ note.user.firstName }}</div>
                                <div class="price"><b>Plat :</b> {{ note.plats.name}} ( id: {{ note.plats.id }} )</div>
                            </div>
                        </div>
                    </div>            
                {% else %}            
                    <div class="cart shadow p-3 mb-5 bg-white rounded">
                        <div class="cart-content p-4">
                            Aucune note 
                        </div>
                    </div>         
                {% endfor %}
        
            </div>
        </div>
    </div>

{% endblock %}
