<nav class="navbar navbar-expand-lg" style="background-color: rgb(20,33,61);">
  <a class="navbar-brand" href="/">PAKI PAKI</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('restaurant_index') }}">Restaurants</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('plat_index') }}">Plats</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('type_index') }}">Type</a>
      </li>
      {% if app.user %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path('app_logout') }}" class="nav-link">Déconnexion</a>
            </li>
        </ul>
      {% else %}
        
      {% endif %}
  </div>
</nav>
<style>
  .nav-link, .nav-itm, .navbar-brand {
    color : white;
  } 
</style>
